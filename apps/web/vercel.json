{
    "version": 2,
    "framework": "nextjs",
    "buildCommand": "cd ../.. && pnpm install --frozen-lockfile && pnpm prisma:generate && pnpm build:web",
    "installCommand": "cd ../.. && pnpm install --frozen-lockfile",
    "functions": {
        "app/api/**/*.ts": {
            "maxDuration": 30
        }
    }
}