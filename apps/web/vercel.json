{
    "version": 2,
    "framework": "nextjs",
    "buildCommand": "cd ../.. && pnpm install --frozen-lockfile && pnpm prisma:generate && cd apps/web && npm run build",
    "installCommand": "cd ../.. && pnpm install --frozen-lockfile",
    "functions": {
        "app/api/**/*.ts": {
            "maxDuration": 30
        }
    }
}